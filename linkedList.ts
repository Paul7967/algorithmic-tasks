class LinkedListNode {
    constructor() {
        this.value = value;
        this.next = null;
    }

    toString() {
        return `${this.value}`;
    }
}

class LinkedList {
    constructor() {
        this.head = null;
        this.tail = null;
    }
}

export const makeSingleLinkedListFromArray = (inArr: []) => {
    const result = {};
};
